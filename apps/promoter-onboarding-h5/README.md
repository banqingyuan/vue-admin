# Promoter Onboarding H5

- Routes and UI map to Figma nodes as defined in PRD.
- Result QR is a placeholder component; replace later when target is decided.
- API:
  - POST /api/admin/auth/send-code
  - POST /api/admin/auth/login
  - POST /api/promoter/submit-info
  - GET  /api/promoter/status

Dev:
```bash
pnpm -C apps/promoter-onboarding-h5 dev
```


